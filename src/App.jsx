import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { RocketIcon, CoinsIcon, UsersIcon } from "lucide-react";

// ... Canvas içeriği burada devam edecek (kısa tutmak için kesildi)
